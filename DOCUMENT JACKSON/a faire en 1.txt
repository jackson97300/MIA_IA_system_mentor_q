🔥 TOP 5 FICHIERS CRITIQUES (Ordre strict)
1️⃣ config/automation_config.py (Priorité ABSOLUE)
python# POURQUOI EN PREMIER :
# - Foundation de tout le système
# - Aucune dépendance locale
# - Tous les autres fichiers en dépendent
# - Configuration centralisée sécurisée

# CONTENU (~150 lignes) :
@dataclass
class AutomationConfig:
    # Trading parameters
    enable_live_trading: bool = False
    max_position_size: int = 2
    daily_loss_limit: float = 500.0
    
    # Risk management
    emergency_stop_enabled: bool = True
    max_drawdown_percent: float = 15.0
    
    # Data collection
    enable_detailed_snapshots: bool = True
    snapshot_frequency: str = "every_trade"
2️⃣ execution/simple_trader.py (Core automation)
python# POURQUOI EN DEUXIÈME :
# - Core du système automation
# - Dépend seulement de automation_config + vos modules existants
# - Orchestrateur principal de votre Battle Navale
# - Base pour tous les autres composants

# CONTENU (~400 lignes) :
class SimpleBattleNavaleTrader:
    def __init__(self, mode="PAPER"):
        self.config = get_automation_config()
        self.battle_detector = create_battle_navale_detector()
        # ... automation logic
    
    def run_trading_session(self):
        # Loop principal automation
3️⃣ execution/trade_snapshotter.py (Data collection)
python# POURQUOI EN TROISIÈME :
# - Utilisé par simple_trader pour capturer données
# - Dépend de automation_config + base_types
# - Critical pour Phase 3 (data = or pour ML)
# - Peut fonctionner indépendamment pour tests

# CONTENU (~300 lignes) :
class TradeSnapshotter:
    def capture_complete_trade_cycle(self, market_data, signal, result):
        # Capture détaillée chaque trade
        
    def export_training_data(self):
        # Export pour ML futur
4️⃣ automation_main.py (Point d'entrée)
python# POURQUOI EN QUATRIÈME :
# - Point d'entrée principal système
# - Utilise simple_trader + snapshotter
# - Permet de tester les 3 premiers fichiers ensemble
# - Interface utilisateur du système

# CONTENU (~150 lignes) :
def main():
    trader = SimpleBattleNavaleTrader()
    trader.run_trading_session()

if __name__ == "__main__":
    main()
5️⃣ monitoring/live_monitor.py (Surveillance)
python# POURQUOI EN CINQUIÈME :
# - Surveillance temps réel du simple_trader
# - Sécurité et monitoring critique
# - Détecte problèmes automation
# - Alertes précoces

# CONTENU (~300 lignes) :
class LiveMonitor:
    def __init__(self, trader: SimpleBattleNavaleTrader):
        self.trader = trader
        
    def monitor_system_health(self):
        # Surveillance continue système